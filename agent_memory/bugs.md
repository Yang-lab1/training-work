# 问题与风险

- 真实 AI / ASR provider 是否实际调用仍取决于生产环境变量；无 Key 时会明确使用 mock/fallback。
- 虚拟面试官当前为静态生成图像，不包含真实视频或口型同步。
- 当前“模拟面试”仍是分段录音 + 自动转写/自动分析闭环，不是真正的 WebRTC 实时通话；UI 已改成通话语义，但底层仍不是实时双向语音。
- 岗位资料自动准备仍依赖浏览器本地存储，尚无账号级云端同步。
- Excel 解析继续遵循本地文件、10 MB 限制、不执行公式、不注入 HTML 的隔离策略。
